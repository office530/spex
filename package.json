{
  "name": "spex",
  "version": "0.0.0",
  "private": true,
  "description": "Spex — internal CRM+ERP for an Israeli renovation contractor",
  "packageManager": "pnpm@9.12.3",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "turbo": "^2.2.3",
    "typescript": "^5.6.3"
  }
}
