{
  "name": "@spex/ui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "shadcn/ui + Radix + Tailwind component library (RTL-first)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "echo '[ui] lint not yet scaffolded' && exit 0",
    "typecheck": "tsc --noEmit",
    "test": "echo '[ui] tests not yet scaffolded' && exit 0",
    "clean": "rm -rf dist .turbo"
  }
}
