{
  "name": "@spex/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Zod schemas, enums, and types shared between web and api",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "echo '[shared] lint not yet scaffolded' && exit 0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist .turbo coverage"
  },
  "devDependencies": {
    "vitest": "^2.1.4"
  }
}
